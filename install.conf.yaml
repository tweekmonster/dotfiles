- clean: ['~']

- link:
    ~/.zshrc:
        relink: true
        force: true
        path: zshrc
    ~/.gitignore_global:
        relink: true
        path: gitignore
    ~/.gitconfig:
        relink: true
        path: gitconfig
    ~/.zshenv:
        relink: true
        path: zshenv
    ~/.vim/bundle/Vundle.vim:
        relink: true
        create: true
        path: misc/Vundle.vim/
    ~/.vimrc:
        relink: true
        path: vimrc
    # ~/.vim/syntax:
    #     relink: true
    #     create: true
    #     path: misc/vim_syntax
    ~/.vim/plugin:
        relink: true
        create: true
        path: misc/vim_plugin
    ~/.vim/after:
        relink: true
        create: true
        path: misc/vim_after
    ~/.vim/autoload:
        relink: true
        create: true
        path: misc/vim_autoload
    ~/.tmux.conf:
        relink: true
        path: tmux.conf
    ~/.npmrc:
        relink: true
        path: npmrc

- shell:
    -
     command: git submodule update --init --recursive
     stdout: true
    - rm -f ~/.zcompdump*
    - mkdir -p ~/.vim/undo
    -
     command: ./post_install
     stdout: true
